import React from "react";
import Container from "./styles.searchlist";

const SearchList: React.FC = (props: object) => (
  <Container>
    <h1>SearchList</h1>
  </Container>
);

export default SearchList;
